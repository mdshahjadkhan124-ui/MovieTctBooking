# Backend API base URL, including the /api prefix
# (e.g. https://your-backend.onrender.com/api in production).
VITE_API_BASE_URL=

# Stripe publishable key (pk_test_... or pk_live_...) — safe to expose
# client-side, unlike the secret key.
VITE_STRIPE_PUBLISHABLE_KEY=
